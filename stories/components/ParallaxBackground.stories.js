import { createParallaxBackground } from "./ParallaxBackground";

export default {};

const Template = () => createParallaxBackground();

export const ParallaxBackground = Template.bind({});

//TODO: why is the title on this one in a subfolder?